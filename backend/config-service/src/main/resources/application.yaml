server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/DiegoRamirez2/config_data_PEP2
          search-paths: config-data
  application:
    name: config-service